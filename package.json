{
  "name": "ai-for-everyone",
  "version": "1.0.0",
  "description": "《人人都能懂的 AI》——面向普通大众的中文 AI 基础原理通识知识库",
  "scripts": {
    "serve": "honkit build && node scripts/inject-lang-switcher.mjs && node scripts/serve-preview.mjs",
    "serve:dev": "honkit serve",
    "build": "honkit build && node scripts/inject-lang-switcher.mjs && node scripts/inject-seo.mjs",
    "clean": "honkit build --clean",
    "generate-svg": "node _illustrations/generate.mjs"
  },
  "license": "CC-BY-SA-4.0",
  "devDependencies": {
    "honkit": "^6.0.0"
  }
}
